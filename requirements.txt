# Pinned for a stable multi-day run. Adjust the torch build to your CUDA version.
# Qwen3 requires transformers >= 4.51; we float slightly above for safety.

torch>=2.4.0
transformers>=4.51.0,<5.0
datasets>=2.21.0
peft>=0.12.0
trl>=0.9.6
accelerate>=0.33.0
bitsandbytes>=0.43.3
sentencepiece>=0.2.0
pyyaml>=6.0.2

# Day 2 data pipeline
requests>=2.32.0
beautifulsoup4>=4.12.3
hf-transfer>=0.1.6

# Tracking (optional; either works)
wandb>=0.17.7
mlflow>=2.15.1

# Serving (Day 6)
vllm>=0.5.4

# Dev / tests
pytest>=8.3.2
